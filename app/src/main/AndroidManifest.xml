<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.menu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Menu">
        <activity android:name=".authentification.LoginActivity"></activity>
        <activity android:name=".authentification.RegistrationForm" />
        <activity android:name=".menu_activities.MenuActivity4" />
        <activity android:name=".menu_activities.MenuActivity3" />
        <activity android:name=".menu_activities.MenuActivity1" />
        <activity android:name=".menu_activities.MenuActivity2" />
        <activity android:name=".menu_activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>